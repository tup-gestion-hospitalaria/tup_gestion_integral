import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gestionhospitalaria.app',
  appName: 'Gestión Hospitalaria',
  webDir: 'dist/frontend/browser',
};

export default config;
